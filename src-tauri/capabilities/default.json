{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for app windows",
  "windows": ["main", "capture"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
